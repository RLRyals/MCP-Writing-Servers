# ==========================================
# MCP Writing System - Configuration
# ==========================================

# ==========================================
# PostgreSQL Database Configuration
# Uses your existing database container
# ==========================================
POSTGRES_DB=mcp_series
POSTGRES_USER=writer
POSTGRES_PASSWORD=your_secure_password2025
POSTGRES_CONTAINER_NAME=mcp-series-db-2
POSTGRES_PORT=5432

# Database connection URL (auto-generated from above settings)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://writer:your_secure_password2025@localhost:5432/mcp_series

# PostgreSQL Performance Tuning (optional - defaults are good)
POSTGRES_SHARED_BUFFERS=256MB
POSTGRES_EFFECTIVE_CACHE_SIZE=1GB
POSTGRES_MAINTENANCE_WORK_MEM=128MB
POSTGRES_CHECKPOINT_COMPLETION_TARGET=0.9
POSTGRES_WAL_BUFFERS=16MB
POSTGRES_MAX_WORKER_PROCESSES=4
POSTGRES_MAX_PARALLEL_WORKERS_PER_GATHER=2
POSTGRES_EFFECTIVE_IO_CONCURRENCY=200

# ==========================================
# MCP Connector Configuration
# ==========================================
# Authentication token for MCP Connector (generate a secure random string)
# You can generate one with: openssl rand -hex 32
MCP_AUTH_TOKEN=change-this-to-a-secure-random-token-32-chars-minimum

# MCP Connector port (Typing Mind standard)
MCP_CONNECTOR_PORT=50880

# HTTP/SSE Server port (for connector-http-sse mode)
# Separate from Typing Mind web UI port to avoid conflicts
HTTP_SSE_PORT=3001

# Include optional author server from mcps/ directory
INCLUDE_AUTHOR_SERVER=true

# ==========================================
# Node Environment
# ==========================================
NODE_ENV=development

# ==========================================
# Typing Mind Static Web Server (Optional)
# ==========================================
# Port for Typing Mind web interface
TYPING_MIND_PORT=3000

# Path to Typing Mind static files (relative to docker-compose.yml)
TYPING_MIND_DIR=./typing-mind-static

# ==========================================
# Legacy Settings (for backward compatibility)
# ==========================================
# These are kept for older scripts that may reference them
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=3500
