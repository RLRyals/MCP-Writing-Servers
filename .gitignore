# Dependencies
node_modules/

# Typing Mind static files (large - can be re-downloaded from GitHub)
# Source: https://github.com/TypingMind/typingmind
typing-mind-static/*
!typing-mind-static/README.md

# Environment files with secrets
.env
.env.local
.env.*.local
credentials-backup.txt

# Files that may contain tokens/secrets
**/config/secrets.js
**/*.secret.js

# Database backups (potentially large and may contain sensitive data)
backups/
*.sql
*.sql.gz
*-manifest.json

# Exception: Allow SQL files in migrations directory
!migrations/*.sql
!init.sql
